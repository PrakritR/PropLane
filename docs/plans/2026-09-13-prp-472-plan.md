# PRP-472 - clear read state for a viewed merged conversation

## Phase and baseline

Astra plan, September13. User explicitly authorized backlog development, separate branches, akhil-feature-cycle, Linear transitions. PRP472 is In Progress. Created keeper akhil/prp-472-inbox-unread at origin/main7b3d464fe2f102a527949044fc0289513d50029f; no code changes there yet. PRP470 is executing separately in pool6 on akhil/prp-470-inbox-loading and changes shared list/store files. Execute PRP472 after PRP470 review, either stack its branch on reviewed PRP470 or integrate that dependency explicitly; record starting HEAD in handoff. Never overwrite other work, force push, open PR, promote or invoke no-mistakes.

Read repo AGENTS, Akhil instructions, communication-inbox.md, UI checklist, Next local guides and root docs/agents/akhil-feature-cycle.md. Source paths below relative to pool6. Current root checkout has unrelated dirty work; plan only lives here temporarily for transfer.

## Reproduction: verified, real dev/test

Basic phone SMS-notice-only thread on current main WORKS: opening2 unread with SMS UI disabled clears badge and persists across reload. Do not replace this working path casually.

Remaining bug confirmed in merged email+SMS phone row with SMS UI enabled:

1. Dev project emstjswhotsnyksqhqyf only, canonical manager@test.proplane.local login from QA fixture. Pinned localhost3009, flags SMS_COMM_UI_ENABLED=true and managed runtime/scheduler=false. No provider sends, no demo.
2. Fixture inbox id prp472-dev-merged-read, resident@test.proplane.local, two inbound email turns, unread:true, explicit smsConversationKey c02c7ffd-50ec-47d0-acf2-82928be6db27:resident:f4290f7c-31c7-469e-b0bb-859f5ce37f46. Native manager_sms_messages fixture id23cd9472-4720-4472-8472-000000000001, inbound PRP-472 SMS probe1, synthetic from+12025550148/to+12025550149. No real message sent.
3. Wait for SMS/list to finish merging, then click phone row labelled PRP472 mixed channel read probe. URL becomes /portal/communication/active/<encoded conversation key>.
4. Open pane displays PRP-472 email probe1 and probe2; selected phone row still shows blue2 unread. Screenshot output/playwright/prp472-merged-before.png, visually inspected by root. Authenticated GET /api/portal-inbox-threads?scope=axis_portal_inbox_manager_v1 returns fixture unread:true after viewing.
5. Evidence captured before PRP470 UI implementation; its store had begun changing but merged read path remains untouched. Screenshot also has six separate PRP470 fixtures; don't conflate their badges with this selected row.

The API returns native fixture residentEmail:null despite explicit binding. Parent list correctly merges via smsConversationKey; thread pane looks native data up ONLY by email and therefore omits the SMS turn. A numeric badge can clear while SMS stays unseen. The read fix must pass the explicitly selected SMS source into the combined pane, using trusted existing conversation/member keys, so it marks only content actually displayed. Do not infer identity from names, body text, or a shared phone.

## Root cause and decision

pro-unified-inbox.tsx chooses ResidentDirectChatPane when selected row spans channels and has personEmail. pro-resident-detail-inbox.tsx builds the combined timeline but has no read/open persistence or parent callback. Existing ManagerInbox markReadSilent and ManagerSmsPanel markOpened are bypassed. Basic phone-notice path already works.

Fix the combined pane's read contract. Preserve current grouping, visible history and authorization. Reuse existing stores and SMS opened-ID semantics; extract a small shared SMS read-state utility from the existing localStorage implementation if needed so direct pane and ManagerSmsPanel/parent observe the same change. Persist opened IDs synchronously before notifying list, avoid effect/update loops and avoid network refetch for local SMS read changes. Scope new state by authenticated viewer where existing cache supports it; do not expose prior viewer state. Maintain current device-local SMS semantics rather than inventing cross-device receipts.

Resolve the selected native SMS source by explicit conversation/member key first, respecting owner/role boundaries already in resolved payload. Use existing email match only where no explicit binding exists and it is the established exact identity; do not attach another resident/owner's SMS. Test native missing email + explicit binding, and mismatched/shared phone as negative cases.

Email read persistence must not submit merged timeline as one whole-row replacement. Mark only displayed non-trash email members read, preserving server history, ownership, folder and concurrent arrivals. Prefer a narrow metadata-only mark-read action in the existing /api/portal-inbox-threads route, with a client helper in the existing store/mutation module. Authenticated context and server-authorized member resolution own IDs; request IDs/observed state are not authorization. No raw SQL, no new route/framework or migration. Use established compare-and-set updated_at pattern to prevent lost updates. Read acknowledgement should be tied to observed message/member state; a concurrent unseen inbound arrival must remain unread. No broad replacement, deletion, body rewrite, aiDraft mutation or archive mutation from simply opening.

Because store rows can be collapsed, carefully map observed members/root-message identities: do not clear unseen new member rows merely because same email/phone. Root text is immutable in normal append path; reuse inboxThreadMessages/sourceThreadIds conventions. If the narrow safe path needs a materially different architecture, return planning blocker with specifics, not a silent risky shortcut.

Optimistically clear badge immediately when the displayed messages are read, then reconcile on failure without clobbering newer arrivals. Show concise recoverable error if persistence fails. Do not continuously retry on render or force-fetch; preserve PRP470 initial readiness. Active visible newly-arriving messages can remain cleared under existing read-on-view semantics; hidden/background or different thread arrivals must stay unread. Reopening and reloading must match saved reality.

## Scope and ownership

Terra: selected-member wiring in pro-unified-inbox.tsx, combined pane read synchronization, shared SMS read utility and minimal existing panel integration, metadata-only inbox read helper/server action as necessary.
Luna: independent behavioral regression/auth/concurrency tests, with explicit test file ownership. Reuse store/client/mock patterns and server CAS fake DB. Sol owns integration, browser fixtures and full validation.

Exclude pricing/tours/agent changes, sender identity redesign, outbound transport, dashboard counts redesign, folders, general read-receipt infrastructure, historical data backfill, production data/schema. Test target fixture only; preserve PRP470 fixtures and unrelated messages.

Observability: retain existing data-attr. No funnel, PII or agent/Langfuse changes needed. No navigation structure or native fork; shared webview UI should work with same changes.

## Acceptance and test matrix

- Selected merged phone row with2 email unread + native SMS: full selected timeline visible; blue unread badge clears immediately without refresh. RTL interaction + seeded browser screenshot, server saved read state.
- Basic phone-notice-only and ordinary email/native SMS selection remain correct; existing manager-sms-panel-opened-loop, sms-inbox-notice-concurrency, unified list/read-count regressions.
- Leave/return and reload stay read; new inbound while visible vs another selected thread/hidden tab follows chosen documented rules. Repeated open is idempotent and no extra network loop.
- Exact selected SMS binding resolves without email; shared phone/other owner or role never merges/marks unrelated data. Unit identity + DOM evidence.
- Server unauthenticated403/401, wrong owner/member IDs denied, read co-manager vs edit/read authority consistent with existing repository rules; never mutate someone else's unread flags through supplied IDs.
- Concurrent append during read CAS preserves all history and marks unseen content unread; stale snapshots cannot overwrite body/messages/drafts/folder. Read request failure rollback doesn't overwrite subsequent message state. Unit/integration behavior.
- Collapsed source members and historical deep link read only messages shown, preserve mixed inbound/outbound ordering and attachments. Hidden/archived rows unaffected.
- Desktop and390px actual manager route, unread list behavior after badge clears, keyboard open, repeat action, error/retry. No demo.
- PRP470 loading/readiness cases pass after integrating shared files. Targeted tests then repo full unit/lint/typecheck/build with exact exits, scoped integration as changed API warrants. No promotions, full E2E before any future promotion.

## Handoff

Save execution handoff and fresh Astra review with base/current HEAD, files, commands/exit codes, exact fixture routes, screenshots, remaining risks. Root coordinates at most2 correction cycles. Run graph hook-rebuild and portable-check if artifacts generated; don't fabricate graph success if CLI incompatible. Commit/push keeper only after review, generate sandbox:open /portal/communication Review URL, Linear Ready for staging only once actually reviewed/validated. Staging/Production stay tied to real deployment.
# Root execution authorization and pinned integration baseline

Execute on `akhil/prp-472-inbox-unread` in pool6, starting HEAD `8cfe3491372515bb2acd96a346ce688047a83ca6`. PRP-470 is reviewed, committed, pushed and Ready for staging. Read this historical plan together with the safety and upstream addenda; their later decisions supersede the original no-migration exclusion and old baseline assumptions.

First integrate pinned main `203d5e58f3ad99e6a977d65b1bbdb69115c52711` using a normal keeper merge with `--no-commit`. Terra resolves the five documented conflicts, Luna independently verifies contracts/tests, and Sol inspects all resolutions and automerges. After the focused integration checks pass, root explicitly authorizes **one local integration-only merge commit** to preserve both parents and give PRP-472 an immutable baseline. This narrow checkpoint exception replaces earlier blanket delegate no-commit wording for this integration only. Do not push it. Record its SHA and resolution inventory before making PRP-472 feature edits. Final fresh Astra/security/Bugbot review must cover the merge against both parents and the unread fix against this checkpoint. Root alone commits the final feature and pushes the keeper after review. Do not chase a later upstream tip mid-cycle.

The safety addendum authorizes exactly one additive observed-read RPC migration applied only to dev/test through the validated isolated CLI history directory and repository `db:push` script. Root's latest read-only catalog check found only `change_portal_inbox_thread_folders(text[],text,text)` among public inbox functions; it does not implement observed reads. No production or staging apply, migration-history repair, unrelated migration replay, provider send, PR, protected branch write, or no-mistakes is authorized.

Root currently owns dev-server session 94061 on pinned 3009 with SMS UI enabled and managed SMS runtime/scheduler disabled. Coordinate before stopping it for merge/build or taking the browser. Existing PRP-470 and PRP-472 synthetic fixtures remain available. Reconfirm the mixed phone-row bug after upstream integration before implementing its fix; no real provider send is needed. Preserve the unrelated existing draft and all other fixture rows.
