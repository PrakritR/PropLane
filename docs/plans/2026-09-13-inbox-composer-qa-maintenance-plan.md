# Bounded retained composer browser test maintenance

The final frozen product candidate `542d11ca0` has passed10,349 unit tests, lint/build, actual401/403 retry browser QA and fresh Astra/security/Bugbot source review. Targeted retained portal E2E exposed stale fixtures/selectors in the older composer test. Root fixed stale private E2E account configuration and a separate resident navigation measurement race (test-only, reviewed). Do not change production implementation to satisfy this outdated test.

Workspace pool6 `/Users/akhilvemuri/.treehouse/AXIS-2-ea44f0/6/AXIS-2`, keeper `akhil/prp-472-inbox-unread`. Source remains frozen except root mechanical `tests/e2e/mobile-portal-layout.spec.ts` measurement retry. Current E2E log `/private/tmp/axis-inbox-release/portal-e2e.log`, error-context under test-results/communication-reply*/. Root owns server3009, browser and real data. No no-mistakes/Git/providers/DB/browser or server actions by implementers.

`tests/e2e/communication-reply-composer.spec.ts` second test expects a Test Resident list label, direct-pane textarea data-attr, always-visible mobile emoji,40px attachment, and a Schedule checkbox. Current shared direct pane uses keyboard emoji on small screens and a schedule button/menu. The synthetic pure-email Test Resident fixture root created naturally uses generic ManagerInbox, so it cannot prove this direct-pane test. Use an optional environment-provided conversation list label (default existing Test Resident for CI) and root will supply `PRP-472 SMS probe 1` for the real existing mixed email/native fixture, without modifying its phone name or binding. Rename test description to reflect resident composer generally. Preserve direct-pane data-attr/form scope so the test cannot silently pass the wrong pane. Test real click/focus, draft, visible desktop emoji and intentionally hidden phone emoji, filechooser, send enabled/disabled without submission, actual schedule button/menu configure/cancel. Do not select/upload files or send a message. Do not weaken geometry guarantees arbitrarily; prioritize actual control clickability and current intentionalphone UI. Inspect current InboxComposer and InboxComposerScheduleMenu to follow real controls. Clear any initial draft before disabled-state assertions.

Fresh Sol-medium delegates Terra only the composer spec maintenance and Luna independent read-only current UI/fixture review. No overlap or new product code. Root will remove its isolated pure-email QAfixture separately if no longer needed. Root reruns targeted actual browser suite and scopedlint/typecheck; implementers can run scopedlint/typecheck only once if cheap. Retain a concise handoff `docs/plans/2026-09-13-inbox-composer-qa-maintenance-handoff.md`; no productioncode changes, no newbroadtestcycle. Finish promptly so root can promote the reviewed product candidate after QA.
