# Fresh Astra release review - 2026-09-11

Decision: corrections required before staging apply; production release remains blocked. Keeper preparation may continue locally. This review grants no database, Git, provider, or release authority. The production waiver remains DRAFT.

Reviewed the working tree at keeper `97815a2d459c0bf96e9e80d4f94d29bff7d23fe2`, with the supplied no-commit merge of `8ce3868b4e5775661956c6c3f36fcb146bfa931a`; compared the checkout route against live `2d1353af42c3a652be6cf8a69640468b453f4cea`. Read the installed feature-cycle skill, repository/developer process, release plan, handoff, root evidence, and relevant billing/SMS/database/ship safety material. This is a focused static and unit review, not a broad release certification.

## Findings

1. **High apply-validation gap:** `tests/unit/comms-billing-migration-apply.test.ts:185-209,230-285`. The transaction-order assertions use fake clients; the digest test validates the current fixture rather than exercising rejection of drift. The production-waiver test fails at the process-argument gate and does not reach a valid APPROVED fixture, wrong runner hash, or consumed status. No test drives the actual new runner through real PostgreSQL transaction rollback and TLS failure boundaries or its private CLI subprocess/environment boundary. The older local rehearsal validates the bundle, not this transport. Root's successful read-only staging preflight is useful real TLS/credential evidence but does not exercise apply, auxiliary failure rollback, or uncertain COMMIT. These omissions block applying this privileged one-shot runner. Use isolated fixture copies; the real DRAFT need never be approved for tests.

2. **Medium verification defect:** `scripts/apply-20260911-comms-billing-migrations.mjs:170-174,199-202`. Fresh catalog verification checks table-level CRUD privileges but omits effective column grants (the immutable bundle itself checks them). Index verification uses names and substring matches, without proving uniqueness, validity, owning relation, and exact key/predicate. Constraint substring matches can accept a weakened expression containing the expected words. Consequently fresh `--verify` can declare an altered catalog installed despite the promised exact contract. Add structural catalog checks and negative behavioral fixtures in the new runner only; do not revise the reviewed bundle.

3. **Medium manager authorization boundary:** `src/app/api/stripe/checkout-portal/route.ts:35-43,124-127`. Authentication and a populated profile manager ID are not canonical role authorization. The documented intent and inspected caller (`src/components/portal/pro-plan.tsx:378-390`) are an existing manager upgrade. The newly added service-role billing customer creation makes an explicit manager guard appropriate before that side effect. The identity helper pins reads/writes to the authenticated user, so this is not demonstrated cross-account theft or a High. Add the canonical guard with resident/vendor denial and legitimate multi-role coverage; if this route is deliberately a role-enrollment path, establish that contract before imposing the guard. This narrow boundary correction belongs in this release.

4. **Medium affected UI QA blocker, pre-existing:** `src/components/portal/portal-inbox-ui.tsx:1450,1624-1648`. Root reproduced the shared composer at 375px with a 62px textarea whose center hits the emoji control; 768px and 1280px clicks pass. Source is unchanged from live. This is not a merge regression, but affected phone QA cannot be called successful. Correct only the shared composer layout, preserving all controls, attachments, scheduling behavior, safe areas, and existing phone navigation.

## Positive controls and report dispositions

- Atomicity is correctly structured at runner lines 91-97 and 233-237: validate the full digest, remove exactly `begin;\n` / `commit;\n`, execute the identical interior under one driver transaction, insert the complete bundle as the auxiliary statement, validate, then issue one COMMIT. No retry path was found. Source ledger statements remain exact and historical rows are compared, not repaired.
- Fixed target binding, pinned CLI, private 0700 workspace/0600 config, child environment allowlist, no emitted-shell execution, pinned CA, hostname verification and `rejectUnauthorized` are present at lines 100-146. Async error listeners precede connect; query/connect/cleanup are bounded. Database errors are sanitized. Timeout races do not cancel the underlying operation; real-driver failure tests must demonstrate conservative classification and termination. No concrete secret disclosure was reproduced.
- Clean rollback compares the complete sampled prior state, including volatile activity counts. This may conservatively report uncertainty when unrelated activity changes; it is not a false-success defect. Installed verification with historical preservation and independent TLS readback follows confirmed COMMIT. Uncertain COMMIT remains nonzero even if installed readback succeeds. Keep that behavior.
- The security report's STOP High is **closed in reviewed source and focused regression**: dispatcher line 487 forwards `row.recipient_user_id`; the regression at test line 261 checks the user-keyed changed-phone opt-out and zero campaign allocation, wallet reservation, and provider calls. The allowed-tour regression retains campaign/wallet dispatch. The suppression loader is mocked in this unit test; this is not handset acceptance evidence.
- The security report's subscription idempotency High is **downgraded to Medium, pre-existing follow-up**. The live diff confirms fresh checkout creation already existed without a request key. The caller opens an embedded checkout and marks itself busy; the reported double charge additionally requires completion of two separate sessions. Reusing an owner-verified customer does not itself establish a new automatic duplicate charge. Durable session/idempotency and active-subscription reconciliation remain worthwhile, but the report does not justify expanding this bounded release into that redesign.
- The bugbot campaign-cap P2 is **not accepted as a defect under the stated contract**. Allocation once per outbox/day before carrier work is deliberately conservative across retries and unknown outcomes. A definitive wallet refusal can consume allowance without a send; no evidence establishes a promise that this protective cap counts only submitted messages. Do not reorder or refund it on this report alone.
- Canonical recovery correction attaches the existing guard and capture functions to exactly the two new account-owned histories. Its stale comment calling it a rollout artifact is cosmetic; preserve the exact approved bytes.

## Independent validation

- Ran `./node_modules/.bin/vitest run tests/unit/comms-billing-migration-apply.test.ts tests/unit/sms-conversation-log-dispatch.test.ts --maxWorkers=1 --reporter=dot`: **exit 0, 2 files, 21 tests passed**.
- Independently computed SHA-256 with Node crypto and rebuilt the bundle through the preparation module: preparation `cff0016ba1799b75ddb295b83c66f921c56857e5e7d0f675adcd9c73fd91787b` (15427 bytes); runner `bd4f1f687c02329164e25cc439e0f62a0d699a6bf49f8692c94b076d944b57d7` (26571 bytes); canonical correction `229e0699bc0e4685dc8ba2f6380ef9d2358816a1d72415abe7d4adb7a9c49713` (1579 bytes); CA `700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7`; six-source bundle `c8013155b6cb51792950177cd47b348d51d7ce24df7869a5892ec74bb29541ff` (67040 bytes). All match supplied identities. Consumed recovery artifacts were not modified; their hashes were not independently recomputed in this focused review.
- Scoped local reads and a live-baseline checkout-route diff only. No compiler, full suite, browser, local database rehearsal, credentials, remote calls, Git mutations, or production approval performed here. Broad validation remains root-owned. Root evidence records nightly **52 failures**, not an affected-QA pass.

## Remaining gates

Execute correction-1 and obtain a fresh Astra review (maximum two automated correction cycles overall). Root must finish stable full unit/lint/type/build and seeded affected browser QA, resolve current full-E2E failures, land only through the authorized ladder, establish exact-SHA staging deployment with staging-scoped credentials, and run reviewed staging apply/readback plus full feature QA. Handset receipt/STOP/START/reply acceptance and current/rollback invoicer cutover proof remain outstanding. Then ship preflight, fresh bounded production preflight, and Akhil's exact named waiver plus final runner-hash approval are required. Preparation approval is not staging-apply approval or production release approval. Never infer approval from DRAFT; consume the waiver only after confirmed success and independent readback, never retry uncertainty.
