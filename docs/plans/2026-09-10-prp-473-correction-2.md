# PRP-473 correction cycle 2

Fresh Sol-medium manager with Terra/Luna delegates per Akhil feature cycle. This is correction 2 of at most 2 automatic cycles. Worktree `/Users/akhilvemuri/.treehouse/AXIS-2-ea44f0/3/AXIS-2`; keeper `akhil/backlog-repeat-issues`; base/current HEAD `0b6d56794407277761ad5f6c680a522e97db2e6d`. Preserve the entire existing dirty implementation and unrelated user artifacts. Read the original plan, correction-1 handoff, fresh review-2 and correction-1 security/bugbot reports.

## Required changes

1. Carry the authorized stored manager identity through the actual planned reschedule/cancel notification path. `inquiryFromPlannedEvent` must retain `managerUserId` alongside `smsConsent`/`smsOrigin`, or use an equivalently enforced typed authoritative argument. Both callers must reach the real notifier with the correct owner. Derive identity from the authorized event, never from manager-supplied request input. Confirm the same owner reaches the eligibility resolver, signed Reply-To, canonical inbox append, tracing and proposal recorder. Do not refactor unrelated booking logic.
2. Make legacy proposal repair strictly upgrade-only. Capture the original selected event id, owner, source identity, exact phone pair, window, row generation, proposal version/generation/status/conversation identity and origin evidence. Revalidate eligibility and apply a CAS only if the same actionable snapshot remains. Reject every changed snapshot instead of using the general creation fallback. The repair must never create a proposal, reopen terminal status, replace a rotated sender, or return repaired when it did no repair. Bound retries; preserve the original snapshot throughout them.
3. Require current row generation agreement in planned reply candidate matching and final confirmation/follow-up CAS. Handle genuine generationless legacy rows deliberately and fail closed on a mismatched present generation. Retain active/canceled, manager, phone, original work number, version, window, duplicate-SID, ambiguity and pending-inquiry checks. A new operation may create its own proposal only for the row's current persisted generation, never overwrite a newer operation when an older delivery finishes late.

Root implementation checkpoint clarification: preserving the original snapshot also applies after a successful upgrade. An unconstrained recursive handler call must not retarget the same inbound YES from repaired A onto newer B inserted before that recursive read, or repeatedly repair without a bound. Carry the selected identity through consumption or reject a changed target; add a regression for a change immediately after the upgrade succeeds. This clarifies the existing snapshot/generation requirement, not a new booking feature.

## Tests that must fail before the fix

- Actual `confirmTourInquiry` or `acceptTourInquiry` -> `reschedulePlannedTour`/`cancelPlannedTour` -> real notification helper -> real eligibility resolver. Use hermetic database/provider fixtures, not a mocked resolver or manually reconstructed projection. Include a positive legacy SMS-origin false/missing-checkbox row with exact ledger evidence, explicit form opt-in, and a negative `non_sms` no-opt-in row with matching historical conversation evidence. Assert positive owner/service/conversation propagation, provider invocation and canonical inbox append; assert the negative result is specifically `tour_sms_consent_missing`, not missing scope. Cover both planned change callers. Existing projection tests can supplement this chain.
- Legacy A candidate selected, CAS reread replaces it with confirmed B at the same window. Assert contested read, no stale write, no reopening, no confirmed reply for A, and B unchanged. Also change work number, owner, phone, window, provenance, row generation and terminal status at that reread. Assert failed-CAS retries retain the expected snapshot.
- A normally shaped eligible planned proposal whose generation differs from the row must not accept YES or consume an alternate-time response. A late notification recorder for A must not replace current B even when A and B use the same time window. Keep successful legacy upgrade, current-generation YES, repeat YES, A->B->A, pending-inquiry YES, ambiguous matches, and follow-up delivery-failure behavior.
- At the actual planned notification boundary, configured signed email Reply-To must use the stored manager and sandbox email skip must remain safe. Preserve customized manager prose and conservative preview alignment. No requirement to make the client decide SMS eligibility.

## Real dev/browser acceptance

Use canonical accounts and existing private auth state. Test target must be dev `emstjswhotsnyksqhqyf`; keep `SMS_RUNTIME_ENABLED=0`, no real Twilio/Resend sends. Coordinate server 3008 with root and stop it for build. Never modify Morgan/shared seeds.

The correction-1 task event `dba5219a-2629-4b83-9cb4-aabf15f5a074` is available for another safe task-only reschedule. Its last persisted window is `2026-09-14T21:00Z` to `21:30Z`, inquiry `1116574c-1af4-454b-84b8-de2879378491`, label `PRP473 C1 2107`; inspect before changing it. Its guest address is the seeded manager's sandbox email. Prefer an additional task-unique inquiry attached to the canonical resident's sandbox identity if needed to prove the recipient view. Authorized dev fixture creation is allowed; no new external account is required.

Drive real manager time picker and preview, submit with inbox plus sandbox email and no SMS. Require response `guestNotification.inbox.sent: true`, then inspect the exact durable canonical thread/message id and body, updated event window/generation and settled reload. Where possible open the seeded recipient's Communication thread and verify the new message. Exercise desktop/mobile, no-SMS preview, selected-channel failure and a repeat/stale action. Mutation `ok:true` by itself is insufficient. A pending-path browser pass does not substitute for the planned-path regression; cover both at least in the automated real-caller matrix.

Real handset/provider receipt acceptance and staging remain explicitly pending external evidence. Never infer delivery from outbox acceptance or test mocks, and never use the incident recipient as a test number.

## Scope and gates

Primary ownership: `src/lib/tour-planned-change.server.ts`, `src/lib/tour-reschedule-sms-reply.server.ts`, narrow notification types/callers if needed, and relevant focused tests including the new lifecycle regression. Keep all correction-1 consent/dispatch/provenance changes. No migration, new route, UI redesign, bulk backfill, production writes or policy expansion is expected. Update relevant tour architecture notes only if needed for the new invariant; do not edit copied workflow/backlog/sibling documents.

Run focused regressions first, affected tour/SMS/inbox/email integration tests, then full required unit/lint/typecheck/build once final source is stable. Node22.23.0, 4GB heap, bounded workers, one compiler at a time. Report exact commands/exits and distinguish earlier runs from final-diff evidence. Keep established graph hook/portable-check incompatibility explicit; do not commit legacy graph sidecars.

Leave `docs/plans/2026-09-10-prp-473-correction-2-handoff.md` with file scope, behavior/test evidence, exact fixture/message ids and browser screenshots, limits and review prompt. Root launches one final fresh Astra review with new mandatory security-review and bugbot reports. Do not commit/push before it, do not merge/promote/open PRs or tracker comments, and never invoke no-mistakes. If the final review still finds defects, report them rather than initiating a third automatic cycle.
