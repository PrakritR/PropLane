# PRP-473 correction cycle 1

Owner: fresh Sol-medium implementation manager, with Terra/Luna delegates per Akhil feature cycle. Worktree `/Users/akhilvemuri/.treehouse/AXIS-2-ea44f0/3/AXIS-2`, branch `akhil/backlog-repeat-issues`, base/current HEAD `0b6d56794407277761ad5f6c680a522e97db2e6d`; retain the existing uncommitted implementation. Read the original PRP-473 plan, execution handoff, fresh review, and both dated security reports. This is correction 1 of at most 2 automatic cycles.

## Required corrections

1. Preserve `smsOrigin` through `confirmTourInquiry` planned-event creation and `inquiryFromPlannedEvent`. New voice/email/web-no-opt-in requests must remain ineligible for historical conversation evidence after confirmation, reschedule, and cancellation. Legacy truly absent origin remains eligible only through exact positive evidence. Do not backfill or rewrite historical flags.
2. Represent whether a tour-purpose grant derives from manager-conversation evidence. Revalidate every such grant, including `twilio_start`, against current exact owner/phone/service/class/conversation evidence. Preserve original evidence timestamps. Add the same validation at the managed dispatch policy boundary so a delayed outbox item cannot outlive revocation of its source authority. Scope the change to tour lifecycle purposes/derived evidence; do not alter unrelated application or control-message consent semantics. Keep explicit tour opt-in and independently restored purpose consent distinct from conversation-derived START grants, and keep global/purpose revokes fail-closed. Prefer the existing evidence JSON and a shared helper over a second policy engine; no migration is expected.
3. Support already-persisted pending/planned reply proposals safely. Revalidate legacy evidence and CAS-upgrade only the eligible actionable proposal, preserving its original work number, generation/version, phone, window and status. Same-generation retries must not falsely report repaired evidence and must never reopen terminal proposals. Reject changed owner/phone/window/generation, canceled rows, missing evidence and unreadable ledgers. Repair the existing CAS-race fixture so it reaches the intended contested read and asserts that it did.
4. Make default confirmation instructions truthful for selected channels and available reply paths. Email-only or unavailable SMS must not imply an actionable SMS YES exists. Promise an email reply to the team only when a valid signed Reply-To (or deliberately verified existing equivalent) is attached. When unavailable, provide an existing reachable action using known server-derived context, such as the established account-creation/contact flow; do not invent a new mailbox or expose an unapproved private address. Keep preview and delivered default copy aligned without trusting client eligibility as authorization, and preserve genuinely customized manager prose. Add behavioral coverage for configured and unconfigured reply domain/secret, guest without an account, selected/no SMS, invalid token, and sandbox skip. Inspect actual outbound payloads and inbound manager-thread persistence; source-string assertions alone are insufficient.

## Exact permitted execution scope

- Existing PRP-473 source/test files from the handoff.
- `src/lib/tour-inquiry-confirm.server.ts`, `src/lib/tour-planned-change.server.ts` for provenance projections.
- Narrow caller extension identified during integration: `src/lib/tour-inquiry.server.ts` also creates a planned event in live `acceptTourInquiry`, called by the registered calendar `acceptTourInquiryTool`. Carry the same stored `smsConsent`/`smsOrigin` there and add behavioral projection coverage. Do not use this correction to refactor the duplicate booking core. The no-opt-in non-SMS guarantee must survive either confirmation path.
- `src/lib/sms/owner-sms-dispatcher.server.ts` and, only if needed to share evidence handling, `src/lib/sms/application-consent.server.ts` or `src/lib/sms-consent.ts`. No unrelated policy changes.
- `src/components/portal/pro-tours.tsx` and existing tour notification context/types/callers only where needed to align truthful defaults. Read the current UI checklist and relevant local Next.js guide before frontend edits; apply appropriate frontend skill without redesigning the portal.
- Focused behavioral tests around the above and existing inbound email helper tests. Update `docs/agents/sms-system.md` where it still incorrectly states that only `smsConsent===true` authorizes tour SMS, and the tours note for legacy compatibility/derived authority.
- New correction handoff under `docs/plans/`. Fresh reviewer owns follow-up review reports. Do not edit unrelated copied workflow context, backlog queue, or PRP-469 investigation.

## Acceptance and verification

Map each finding to a regression that fails on the current diff. Required behavior: non-SMS create -> confirm -> reschedule/cancel remains blocked despite a matching old conversation; old SMS-origin legacy still sends through actual resolver; each trusted conversation source is denied after source revoke at retry and delayed dispatch; independent purpose consent remains deliberate; old proposal YES works only with safe revalidation; no terminal reopening; wrong phone/owner/work number/generation remains blocked. Use provider mocks, not a mocked-away eligibility resolver, for lifecycle/dispatcher contracts. Include a CAS-race test that proves the race was exercised.

Verify actual email payload Reply-To and manager-thread reply persistence with a guest lacking a profile, plus absent-config copy and invalid-token fallback. Drive the real seeded manager browser time-picker/preview and safe sandbox mutation, inspect route response and durable row after reload, and check desktop/mobile. Reuse canonical accounts/private states; save refreshed authentication privately. Read `2026-09-10-prp-473-root-verification.md`: shared Morgan seed data changed after Sol QA. Use a task-unique safe dev inquiry id and inspect both the singleton and any normalized event row after server sync; do not reset other actors' seed data or rely on stale local storage or `/demo`.

Run focused tests and affected SMS/outbox/tour/email integration tests, then required unit/lint/typecheck/build checks. Use Node22.23.0, bounded test workers, and one compiler at a time. Coordinate the pinned3008 server with root; never build while its dev server is running. Record exact commands and exit codes. Keep provider credentials/runtime sends off; no Twilio/Resend send or production write. Graph refresh follows repository tooling if available; otherwise document the established CLI blocker without committing legacy artifacts.

Retain accepted-versus-sent outcomes, fail-soft PII-free lifecycle tracing, no no-mistakes, no tickets/comments/PRs, no protected merges/promotions. Do not mark issue resolved or handset QA passed. Return the correction handoff to root for a fresh Astra review; do not commit or push before that review.
