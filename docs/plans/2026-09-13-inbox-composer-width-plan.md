# Inbox composer narrow-phone correction

The final real DEV E2E measures the actual resident direct-pane textarea at 129px on a 375px viewport. The preserved >140px acceptance assertion fails. The shared composer has attachment, field, three 36px tools and Send in one row. SMS UI off does not remove the channel menu, so changing flags is not a remedy.

Use the Akhil feature cycle for this bounded discovered UI defect. Root owns browser/server/release. A fresh Sol-medium manager delegates implementation and independent checks to Terra/Luna. Read the matching developer instructions, Communication architecture, UI checklist, frontend guidelines and installed Next docs before editing.

Keep the existing composer structure and action sizes. First assess reducing only phone spacing when trailing controls are present: row gap from6px to4px and tool gap from4px to0px recovers14px without hiding/shrinking controls or changing desktop. If this cannot preserve readable, clickable layout, report the needed reflow rather than weakening the E2E assertion. Preserve safe-area behavior, emoji responsive behavior, send semantics, analytics, authorization and all existing props. No API, database, provider or unrelated UI changes.

Allowed implementation scope: shared InboxComposer in src/components/portal/portal-inbox-ui.tsx, an existing meaningful composer regression test only if appropriate, and this correction handoff. Existing E2E remains the real geometry proof; do not mirror class strings in new tests. Root will rebuild, drive the 12-case portal/composer/mobile suite including375/768/1280, inspect screenshots, and run required unit/lint validation on final source. Fresh Astra reviews the exact diff and final evidence. No commits, pushes, server or DB actions by delegates.
