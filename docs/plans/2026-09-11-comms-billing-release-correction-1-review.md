# Fresh Astra correction-1 review

Decision: bounded test/CI corrections required before declaring correction-1 complete. No new production runtime defect found in the corrected runner, checkout guard, or composer. This review grants no apply, release, Git, or provider authority. The named production waiver remains DRAFT.

Reviewed keeper HEAD `97815a2d459c0bf96e9e80d4f94d29bff7d23fe2` with pending merge `8ce3868b4e5775661956c6c3f36fcb146bfa931a`, the release plan/review/handoffs, correction-1 plan/handoff, and updated root evidence. Read the feature-cycle and repository/Akhil instructions. No compiler, full suite, browser server, local PostgreSQL cluster, remote operation, or Git mutation was started by this reviewer. Only this review and the correction-2 plan were written.

## Remaining findings

1. **Medium - new browser regression uses an ineligible fixture.** `tests/e2e/communication-reply-composer.spec.ts:8` opens the assistant thread; line 62 unconditionally requires Schedule for later. Root's actual seeded browser confirms this thread has no scheduling control. Switching only the thread is insufficient: lines 34, 39, and 47 hardcode `inbox-reply-*`, whereas ordinary Test Resident uses `resident-direct-chat-compose-*`. Preserve assistant layout coverage and exercise scheduling on the eligible ordinary conversation using scoped accessible roles/file inputs or the actual observed composer prefix. Do not add product scheduling or rename analytics attributes to satisfy the test. Collection alone is not execution.

2. **Medium - required unit job has an undeclared system dependency.** `tests/unit/comms-billing-migration-apply-local-harness.test.ts:6` unconditionally invokes the local harness. `scripts/testing/comms-billing-migration-apply-local-harness.mjs:38,44,49` invokes `initdb`, `openssl`, and `pg_ctl` through PATH. `.github/workflows/test.yml:17-28` provisions Node and dependencies, but neither PostgreSQL server tools nor their PATH. `npm run test:unit` includes this file. A clean developer checkout without PostgreSQL now fails the unit gate; hosted Ubuntu compatibility is unproven, not an observed GitHub failure. Explicitly provision a fixed PostgreSQL major and OpenSSL in the existing unit job, expose the server binary directory, and document matching local prerequisites. Keep the harness mandatory and keep unit in aggregate check.

3. **Medium - transport failure coverage stops short of its claimed write boundary.** Harness lines 110-118 replace the interior with `pg_sleep` or terminate the backend before executing it. The recorder increments at line 107, so the single-interior assertion at line 136 counts an attempted call, not successful interior execution. These prove real driver timeout/disconnect handling with an empty transaction, but not cleanup of the six pending source writes after those failures. Move both failures to after actual interior completion and before COMMIT, assert the reached boundary and exact no-replay count, and retain fresh independent clean readback. Auxiliary and catalog failure modes already occur after actual writes and are valuable; do not discard them. This is a verification gap, not a demonstrated atomicity defect.

4. **Low - source/bundle drift assertions do not identify the refusal.** `tests/unit/comms-billing-migration-apply-subprocess.test.ts:175-182` checks only exit 1 and absence of credential acquisition. Unlike its CA case, an unrelated early exception also passes. Assert the specific sanitized source-integrity and bundle-digest diagnostics in these copied fixtures. This is a small completion item for the same test-only correction.

## Corrections accepted and evidence limits

- Runner catalog checks now include effective column SELECT/INSERT/UPDATE/REFERENCES for client and service roles. Exact index comparison includes owning relation, validity, uniqueness, key count, full definition and predicate. Relevant usage constraints are compared by complete catalog definitions. The reported final real-PG success is material: the initial missing WHERE clause was found through actual catalog output and corrected. Negative real catalog mutations exercise a column-only REFERENCES grant, wrong index keys, and weakened constraint. No mock-only justification is used for the final expected definitions.
- The fixed transaction still validates the frozen bundle, executes its exact interior once, inserts the complete auxiliary statement, verifies before COMMIT, and independently reads back. Auxiliary and catalog injections roll back pending source writes. Lost-COMMIT-response injection performs a real COMMIT and then throws; fresh installed readback remains uncertain/nonzero and does not replay. TLS fixtures test trusted, wrong-hostname, and untrusted certificates without changing operational pinned CA/hostname checks.
- Copied APPROVED waiver tests reach only the fake pinned CLI boundary; real DRAFT remains untouched. Wrong hash/consumed/acknowledgement cases refuse before credentials. Private directory/config modes, environment allowlist, fixed CLI version/target, cleanup and credential-output sanitization are exercised by subprocess fixtures.
- Checkout now requires the canonical manager actor and matching authenticated user before parsing/price/customer/provider work. It reuses actor.db and binds the customer to user.id. Route tests exercise denial/admission/mismatched actor; the guard itself is mocked, so the route test comment is not independent end-to-end multi-role evidence. The canonical guard source explicitly reads additive profile_roles. No role-enrollment or idempotency redesign is warranted here.
- Composer change is narrow mobile reflow. Root proved ordinary center click/focus/typing/emoji and enabled Send at 375/768/1280; phone textarea grew from 62px to 251px and phone navigation remains reachable. Root also observed ordinary-thread scheduling and a native file chooser, but did not complete the automated per-width attachment/scheduling matrix. These are partial browser passes with a defective new test, not an application scheduling regression.
- Prior STOP finding remains closed; subscription session idempotency remains the adjudicated Medium pre-existing follow-up. Do not reopen conservative campaign allocation ordering.

## Validation and identities

Independent read-only SHA-256 check exited 0: runner `56e88313a0da9b14ef04e4d146392782a79085e80ff04266149e8a6f9be09cc6`, preparation `cff0016ba1799b75ddb295b83c66f921c56857e5e7d0f675adcd9c73fd91787b`, CA `700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7`. Actual waiver is DRAFT and records this runner digest. No frozen artifact was edited. Bundle/correction/consumed-artifact identities remain the handoff's reported values; this reviewer did not recompute all of them. `git diff --check` exited 0. `.graphify/graph.json` is absent; root already records unavailable graph refresh tooling.

Accepted as attributed evidence, not rerun here: Sol final 6 files/53 tests exit 0, standalone real-PG harness exit 0, scoped lint exit 0, and root standalone TypeScript exit 0. Resource contention on an 8GB host justifies avoiding duplicate heavy validation. Root's optional Webpack path failed on unchanged live-baseline code; default Turbopack compiled successfully but the full build was still pending at review time. Full lint was stopped with 143 and must not be called passed. Earlier full-unit success predates correction-1.

Final root update before handoff: normal default production-mode build completed **exit 0**, including TypeScript and 387 static pages. Full lint restarted alone with a 2GB heap and remains pending. The optional Webpack failure does not require an application change for this correction cycle; tests/CI-only corrections do not invalidate the completed application build.

## Release gates

Run the bounded second/final automated correction cycle and fresh review. Root retains stable broad unit/lint/type/build, completed seeded affected browser QA and Review URL, required full-E2E failure disposition, exact-SHA staging deployment with staging-scoped credentials, reviewed staging apply/readback and full billing/tour/recovery QA. Designated handset receipt/STOP/START/reply acceptance and current/rollback invoicer cutover proof remain outstanding. Then ship preflight, fresh bounded production preflight, Akhil's exact named waiver/final-runner approval, one-shot apply with independent readback, and production Vercel/TestFlight distribution verification remain separate gates. No staging or production write/deployment occurred in this review.
