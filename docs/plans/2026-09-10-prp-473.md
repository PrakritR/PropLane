# PRP-473 - SMS tour reschedules from an existing prospect conversation

Phase: Astra investigation and execution-ready plan. Branch `akhil/backlog-repeat-issues`; starting HEAD `0b6d56794407277761ad5f6c680a522e97db2e6d`. Worktree `/Users/akhilvemuri/.treehouse/AXIS-2-ea44f0/3/AXIS-2`. Read queue artifact for setup and priorities. Do not implement sibling issues as independent cycles yet; shared-path protections for confirmation/cancellation belong in regression checks.

## Evidence and root cause

- Linear PRP-473 explicitly repeats PRP-437: reschedule notification missing on handset and email instructs prospect to confirm in an inaccessible inbox. Prior fix is in live HEAD.
- Langfuse production trace `de38a267-97b4-4c4d-b5b1-4636e807e6d2`, observation `ed3c02e5-ee4e-4a42-ad91-9ebac64b4171`, September 10 07:41 UTC: `request_tour` succeeded for September 11 10:00 Pacific, prospect phone ending 5522. No consent field in tool schema or row builder.
- Production READ ONLY: planned event `bdef26aa-5690-4793-98cc-1e5817317d18`, source inquiry `6fec1785-4cec-4bd8-8876-8ba9080a628a`, same manager and phone ending 5522, now September 12 18:00Z, has `smsConsent: false`. Existing scoped `manager_conversation` transactional consent events for that exact manager/service/prospect phone are granted from `recipient_initiated_inbound`, including 07:41:15 UTC before the tour tool call.
- `tourInquiryRowFrom` never persists SMS origin/context. `createTourInquiry` coerces absence into false. `textTourGuest` returns skipped before looking at conversation consent. It also unconditionally inserts purpose consent for true form flags, risking overwriting a prior purpose revoke; fix that narrowly while changing the gate.
- `textTourGuest` builds a conversation key for consent but does not pass it to `sendResidentOutboundSms.openThread`.
- `buildTourRescheduleConfirmRequestBody` still says “Please reply in your PropLane inbox to confirm this new time”. `pro-tours.tsx` uses it to prefill `messageBody`, which the server honors, overriding the corrected server default.
- `tour-reschedule-sms-reply.server.ts` requires `smsConsent === true` in proposal recording and reply CAS predicates, so only changing the send gate would leave historic SMS-origin bookings unable to confirm by SMS.
- Twilio read-only receipts: ordinary agent replies to the same phone are delivered (e.g. `SM65b91838c959f7af61bdd584262cbd70` after request). Several inbound messages have error 11200 despite a delivered reply; do not claim that explains the missing lifecycle notification. This is a separate possible webhook latency problem.

## User-surface reproduction and setup

Seeded dev tours with `node --env-file=.env --env-file=.env.local scripts/seed-manager-tours.mjs`, exit 0. Logged in via browser as canonical manager. Opened `/portal/tours/pending/inquiry-seed-pending-c02c7ffd-c-0` (Morgan Ellis), clicked Reschedule. Playwright session `backlog` on port 3008, private state `/private/tmp/axis-backlog-manager-state.json`. Complete the date-pick/message-preview reproduction before editing. Root is capturing the modal now and will send exact artifact. Root has NOT reproduced real handset delivery in dev: managed runtime is off. Never mark handset acceptance done from a mocked provider.

## Decisions / implementation

1. Introduce one server-owned tour SMS eligibility/consent resolver, reused for notification delivery and reschedule reply eligibility. Use the existing scoped ledger and suppression helpers. Grant is scoped to the actual owner manager, normalized recipient, authoritative messaging service, prospect conversation key, transactional class. Evidence can be the explicit form opt-in or an existing valid recipient-initiated conversation grant for that exact scope. A checkbox false/missing alone is not positive evidence; a bare phone/session/name is never positive evidence. Restrict conversation evidence to trusted source and appropriate scope; don't let unrelated resident/manager/purpose/email/voice data grant SMS. Preserve source provenance and original occurredAt when materializing a tour-purpose grant, check current purpose revoke first, fail closed on read failures, and never override STOP/revocations. No bulk backfill or live row editing.
2. For future leasing-SMS tool requests, bind the touring phone to authenticated SMS sender context. Normalize equivalent formats; reject mismatching supplied phone, or explicitly use context phone with preview consistent with it. Keep email/voice/resident surfaces separate. Do not add a model-callable boolean that authorizes SMS. If origin/evidence is persisted, pass it as a server-only argument to createTourInquiry, never as trusted arbitrary public `incoming` fields. `runLeasingSmsAgentTurn` currently also serves voice and omits channel in scope; thread channel explicitly before treating default scope as SMS.
3. Existing SMS-origin tours must work using provable scoped ledger evidence at send time, without rewriting historical `smsConsent:false` to pretend a form was checked. Keep original phone and owner. Materialize only valid needed purpose grant. Pass exact conversationKey into outbound sender. Return explicit reason when selected SMS cannot send.
4. Update reschedule proposal/reply checks to accept the same validated eligibility/evidence while preserving CAS, owner, exact phone pair, generation, current time window, stale/canceled/wrong-owner protections. Do not weaken those predicates. Persist enough server-owned proposal evidence for retries and sender rotation. Check relevant tests and data flow before choosing fields.
5. Correct pending reschedule copy and preview to direct prospect to reply YES by SMS or suggest another time. Do not promise an unavailable channel: show alternate truthful action when SMS is unavailable; email must not require a nonexistent inbox. Preserve custom manager text where safe; fix default builder/callers, not arbitrary user prose. The pending request remains pending until existing acceptance semantics say otherwise.
6. Preserve transport state accurately: accepted/queued/submitted is not handset delivered. Forward useful provider SID/outbox status if available and avoid generic “Sent via selected channels” for an all-skipped result. Use existing outcome model, not a second delivery framework. Do not poll Twilio inside user mutation waiting for handset delivery. Separate receipt verification in QA.
7. Observability: existing tool tracing remains. Add only necessary ID/status/reason evidence at the lifecycle send boundary through existing Langfuse wrapper patterns, fail-soft and no Langfuse init in tests. Correlate inquiry, purpose, trace/delivery ID and SID where supported; no phone/email/body in analytics metadata. Ensure no PII in PostHog.

Likely files: `src/lib/tour-notification-delivery.server.ts`, a focused resolver under `src/lib/sms/`, `src/lib/tour-reschedule-sms-reply.server.ts`, `src/lib/tools/domains/tours.ts`, `src/lib/tools/context.ts`, `src/lib/agent/leasing-sms-agent.server.ts`, `src/lib/tour-inquiry-create.server.ts` if provenance needed, `src/lib/tour-notifications.ts`, `src/lib/tour-planned-change.client.ts`, narrow `pro-tours.tsx` caller changes only if required. No new table/migration expected.

## Acceptance / test matrix

- Existing granted SMS conversation plus legacy false/missing form flag: reschedule invokes real shared sender with original phone, owner, service, exact conversation key; pending and planned paths tested.
- New leasing SMS request: authentic normalized sender retained, wrong phone rejected, no model-authored consent; voice/email/resident paths cannot inherit SMS permission.
- Explicit web opt-in still sends. Missing consent, another owner/service/role, revoke/STOP, revoked tour purpose, malformed phone, unreadable ledger: no send and truthful selected-channel failure. Unit/route behavioral tests, not source matches.
- Pending UI pick new slot → preview contains new time/property and SMS reply instruction, no inbox-only confirmation; submit dev route and inspect response plus durable nonproduction row. Desktop and mobile.
- Delivered/queued/provider-failed/skipped outcomes remain distinct and don't regress email/inbox channels. Contract tests around real notification helper with provider mocked; do not mock away consent resolver.
- YES confirms latest reschedule; another-time response reaches existing thread/manager; repeat YES idempotent; A→B→A generates a fresh operation; wrong sender, rotated work number, stale generation and canceled tour refuse. Extend existing reply tests.
- Adjacent PRP-475/476 paths use shared consent resolver without breaking date/time/property/instructions or channel selection. Separate issue acceptance still required later.
- Run focused tests, relevant integration suite, lint, typecheck, build using Node 22.23.0 (`/Users/akhilvemuri/.nvm/versions/node/v22.23.0/bin`). Record exact exit codes. Required broader gate per docs/ship-gate.md. No no-mistakes.
- Refresh graph `npx graphify hook-rebuild`; portable-check before including graph artifacts. Never stage branch/worktree/cache sidecars.

## Execution / review handoff

Fresh Sol-medium manages Terra implementation and Luna test/inventory scopes (non-overlapping files). Sol verifies every delegate result and handles integration. Read all required repo/area docs plus current skill. Save `docs/plans/2026-09-10-prp-473-handoff.md` with changed files, exact commands/exits, browser evidence, account references, risks and remaining handset test. Root then launches fresh Astra review, up to two correction cycles. Do not commit unrelated workflow docs copied for context. Do not push/merge protected branches or send to any real number without root coordinating Akhil's dedicated test handset. User permits test login/account reuse but has not identified the live account/number yet. Complete all independently possible implementation and checks first.
