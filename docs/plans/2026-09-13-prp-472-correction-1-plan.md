# PRP-472 correction cycle 1 - 2026-09-13

Implement all findings from [the fresh review](2026-09-13-prp-472-review.md), then return to a fresh Astra review. Branch `akhil/prp-472-inbox-unread`; integration HEAD `3c997bc4f09ef3b97c80cb559e8909830e164868`; existing feature changes remain uncommitted. Read original plan, both addenda, execution/integration handoffs, RPC report, and security/Bugbot reports. Do not restart upstream integration, chase main, alter migration/permissions, invoke no-mistakes, send messages/providers, commit/push, or touch protected branches/staging/production.

Use a fresh Sol-medium implementation manager with Terra/Luna delegation under the feature cycle. Allocate disjoint implementation and test ownership, with Sol integrating the shared snapshot/read-result interfaces. Root owns serial broad checks on the 8GB host. Coordinate before browser/server or bounded dev fixture mutations. Existing exact-one-file RPC is already applied and validated in dev; corrections require no schema apply.

## Implementation contract

1. **Selected snapshot (R2).** Derive rendered email bubbles and candidate observations from one selected authorized non-trash snapshot. Pass that snapshot or a faithful projection to the combined pane instead of independently reloading by the winning email. Keep the winning contact/reply context, sender direction, root/appended bodies, and attachments correct. Exact selected native members still take precedence over unique email fallback. An unresolved explicit key never falls back by email/phone. Multiple native members must all render before all are locally opened. Historical `sourceThreadIds` remain navigation membership, never authority to acknowledge a raw source absent from a GET.
2. **Safe aggregate reconciliation (R1).** An old request must not change a current merged unread aggregate if an unobserved, revised, conflicting, or unresolved member contributes to it. Either retain per-source metadata or fail closed on incomplete coverage; choose the smallest reliable representation. Missing/malformed results do not mean false. Preserve body, preview, time, folder, draft, and attachment references through success and failure.
3. **Observation ownership (R3).** Local native receipts must continue while an email acknowledgement is pending. Tie network pending entries to viewer authority/epoch and exact email observations, and keep cleanup owned by its request. A stale A callback cannot act in B or later A; a valid later-A request cannot be suppressed by earlier-A work. Only latch a pane signature when all represented observations have been handled appropriately. Do not introduce polling/forced-refresh loops.
4. **Failures and partial outcomes (R4).** Capture the prior unread metadata needed to withdraw only each operation's optimistic change; never manufacture unread=true on a previously read source. Preserve committed per-source outcomes if a later RPC/read fails. Handle malformed/partial responses and unknown network outcomes honestly, with at most bounded revalidation where needed. Guard localStorage exceptions so they neither block email POST/reconciliation nor leak pending ownership. Show a concise recoverable error for actual failures with explicit reopen/retry; no stale-viewer toast.
5. **Both collapse branches (R5).** Consistently union server observations and fail closed on conflict through person, assistant, and unified collapse, including successive passes and mixed legacy rows. Genuine multirow assistant collapse must retain body/root-attachment/source alignment. Do not broaden resident write scope.

## Required regression evidence

Add behavioral tests around actual combined-pane rendering and read callbacks; stubbing the pane without `onViewed` is insufficient. Prefer controlled deferred requests and real storage helpers to source-text assertions.

- Old request A success/failure after current [A unchanged, B new] and [A unchanged, B revised] snapshots; unread B and all new content survive. Missing response B and mixed legacy/conflicting metadata fail closed.
- Two email aliases with one explicit native binding: both bodies and their attachments plus SMS render, and only those rendered source observations are posted. Unrelated same-phone/other-owner or role sources remain excluded. Keep correct reply contact context.
- New visible native SMS during pending email POST becomes locally read without a duplicate email request; settle success and failure. Several exact selected native sources all render/open. Unresolved explicit and ambiguous fallback cases do neither.
- A -> B -> A with earlier-A POST pending: current A can retry; stale callback/results/toasts/finally cannot affect it. Hidden document and hidden mobile pane make no receipt or POST; visibility/open creates one bounded attempt. Reopen works after error; Status=Unread removal never opens a different row or loops; Status=Read reflects the same merged state.
- Already-read plus unread selected sources under HTTP 500 retain original truth. A committed / B failed route batch preserves A's successful result and reconciles B independently. Missing/invalid result arrays and rejected localStorage writes release pending state and preserve safe retry behavior.
- Actual multirow assistant collapse with distinct observations, conflicting observations, and root/appended attachments. Include successive-collapse conflict/legacy inputs rather than only passing a singleton through the second function.
- Route co-manager read denied / edit allowed and effective admin viewer, using real scope helpers or a meaningful integration model. Mixed unauthorized batch must write nothing. Retain stale timestamp/JSON, RPC error no-fallback, archived/idempotent, and existing permission tests.

Run focused changed-area tests/lint/typecheck first and provide exact exits. Root then runs one full unit/lint/build pass after freeze. Rerun affected PRP-470 initial-readiness, viewer, SMS coalescing/polling and upstream Status/archive/composer compatibility. The initial full suite passed before correction, so any new failure must be investigated.

Use existing canonical dev fixtures for desktop/390px happy path, visible new arrivals vs hidden, delayed POST/new source, failure/reopen, alias-source rendering, and unread/read filters. No provider send is needed. Any fixture addition must be bounded, attributable, and cleaned or explicitly retained in the handoff; preserve unrelated messages/drafts. Record final-source screenshot/trace evidence and exact runtime flags. Run the repository graph hook once if code changes, report its known tooling failure honestly without repairing global tooling.

Deliver `docs/plans/2026-09-13-prp-472-correction-1-handoff.md` with R1-R5 resolution map, files, test commands/exits, real browser evidence and remaining limits. Fresh Astra/security/Bugbot review must re-review affected code; original findings remain open until that review verifies correction. Root will commit/push the keeper only after all review and normal validation gates pass.
