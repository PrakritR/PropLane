# Inbox release acceptance review

Date: 2026-09-13. Verdict: **SOURCE ACCEPTED. FINAL RELEASE VALIDATION PENDING.**

Fresh Astra review of keeper `akhil/prp-472-inbox-unread`, HEAD `276e1e40a0a9f3f63bee3a87b4fabbbd9edaf54b` plus frozen working delta, against base `75d711053085e340072c605bcb96eaa9416ef87e`. The reviewed inventory contains 33 source, test, migration, and probe paths. Freeze aggregate: `7432b0cc36d65cd4822aa16295e368812194258ad4b4d7fe35b64f11df263ce8`. Reviewer independently hashed every inventory path against `/private/tmp/axis-inbox-release/source-freeze.json`: 33 checked, zero mismatches, command exit 0.

Read repository and Akhil instructions, canonical feature-cycle contract, Communication/Tours notes, ship gate, original PRP-470/472 plans, release-followup handoff, previous final review/Bugbot, and final correction plan/handoff. Graph query was attempted and exited 1 because this worktree has no graph; authoritative notes and direct source/diff review supplied context. No source, Git, database, server, browser, or provider mutation was performed by this reviewer. Only these review artifacts were written. No no-mistakes.

## Findings and correction acceptance

**No new actionable correctness or regression finding in this frozen scope.** Both P2 findings from `2026-09-13-inbox-release-final-review.md` are resolved:

- **R1, explicit Retry after SMS 401/403:** `retryInitialList` in `src/components/portal/pro-unified-inbox.tsx` clears the halt ref and state only for the visible Retry action and returns `loadInitialList()` for the shared Button's loading lifecycle. Automatic polling and refocus still cannot clear the refusal. Current-viewer and request-generation guards remain intact. Retained mounted tests drive each initial 401/403 to a same-viewer successful retry, exactly one additional SMS request, and row reveal. The 401 -> 403 -> 200 sequence also verifies no 20-second poll or refocus retry is installed while refused, and a later explicit retry succeeds.
- **R2, column-limited recovery trigger:** `20260913173000_tour_followup_recovery_guards.sql` now rejects nonempty `pg_trigger.tgattr` for both named triggers. The rollback probe validates and displays the same field. Its retained executable PGlite regression creates the actual recovery function's narrowed `BEFORE INSERT OR UPDATE OF archived OR DELETE` trigger, verifies rejection, rolls back the failed transaction, and checks that no companion trigger leaked. Fresh install, double application, prerequisites, conflicting-trigger rollback, and lifecycle coverage remain.

These corrections match the bounded correction plan. Neither requires source changes before the root's final validation or rollback-only staging rehearsal.

## Whole-candidate assessment

| Contract | Assessment |
| --- | --- |
| Initial membership and UX | Manager waits for inbox, application membership, and enabled SMS; resident waits for inbox and enabled SMS. Successful empty responses become ready, malformed/failed envelopes remain retryable errors, and disabled SMS does not fetch. Selection/deep-link effects wait for readiness. The shared skeleton uses status/busy semantics and reduced-motion support; Retry uses the shared Button and existing analytics attribute. |
| Viewer isolation | Inbox/application loaders guard old completions and promise cleanup. Manager SMS, composer recipients, read callbacks, and direct-send completion carry viewer generations/authority, including A-B-A. A stale authorization response is checked before changing the new viewer's session state. |
| Observed email reads | The existing route derives authenticated edit scope, validates every requested member before writes, and compares the server observation. The service-only security-invoker RPC compares identity, revision, and complete JSON, changes only unread/revision, and does not replace messages, ownership, folders, or attachments. A false compare gets one bounded reread/retry. Missing/foreign batches fail before any source write. |
| Partial results and concurrent state | Confirmed per-source siblings can settle despite another source failing. A `failed` envelope is unknown, as are missing/malformed/network outcomes; withdrawal removes only the operation-owned compatible overlay and never replays captured unread over newer confirmed truth. Exact current observations protect revised arrivals and archived/incomplete groups. Controller and real reconciler tests cover overlapping alias operations and both settlement orders. |
| Selected content and native SMS receipts | Explicit native binding provenance survives both collapse passes and restricts selected/displayed/acknowledged native members. The combined pane receives the authorized email snapshot, preserving mixed directions, root attachments, and source aliases. Opened receipts are viewer keyed; failed durable writes retain volatile display state and retry on explicit reopening. Hidden controlled opens defer to visibility once, without render-driven retry loops. |
| Cache/render/performance | Successful server hydration is distinct from local staging/session hydration. Existing TTL and in-flight sharing remain; direct-send force refresh goes through the established coalescer. No timer-based reveal, broad prefetch, extra polling cadence, images/fonts, or dependency bundle was introduced. Review found no concrete additional performance blocker. |
| Web/native parity | The changed behavior uses the existing shared portal and local receipt storage. No navigation section, push, upload, safe-area, native plugin, Capacitor configuration, or native shell changes are introduced. Existing deep links and mobile visibility constraints remain covered. This is source parity assessment, not a claim of device/TestFlight testing. |
| Recovery wiring | The additive transaction checks prerequisites and exact existing named triggers, including full-column update coverage. It reuses established recovery functions/lifecycle and changes no grants, RLS, recovery manifest, or historical migration. Probe asserts exact catalog, rejects fixture reuse, checks held-write refusal/delete capture/finalization, rolls back, and checks zero fixture leakage. |

The source/test review includes both storage loaders, SMS client/receipt helper, unified merge, manager/resident containers, combined/native panes, initial-state component, read controller/observation/route/RPC, both migrations, rollback probe, and all 16 changed unit-test paths in the freeze. Existing compatibility tests were adjusted to supply the new completion contract rather than removing their behavior assertions.

## Validation evidence and release boundary

Accepted from the final Sol correction handoff, not rerun by this reviewer:

- Explicit Node 22.23.0 serialized focused inbox command: 9 files, 88 tests, exit 0.
- Explicit Node 22.23.0 recovery matrix: 3 files, 41 tests, exit 0.
- Focused ESLint of correction TypeScript/TSX paths: exit 0.
- `npx graphify hook-rebuild`: exit 1, local executable-resolution failure. No refreshed graph is claimed.

The prior integrated real DEV browser evidence remains relevant to unchanged read/receipt source bytes: full mixed email/SMS content, first failed read followed by reopen success without intervening GET, confirmed success followed by failed reopen without unread resurrection, and actual mounted native receipt failure/recovery/remount preserving an unrelated receipt. See `2026-09-13-prp-472-release-followup-handoff.md`.

Root subsequently completed the exact-freeze full unit suite: **1,465 files, 10,349 tests, exit 0, 217.73 seconds**. Full lint exited 0 with 746 existing warnings in 91.34 seconds. Build exited 0 with the DEV database assertion in 60.34 seconds. These results replace the earlier 1,465-file/10,345-test broad pass, which belongs to the previous freeze. They were reported by root, not rerun by this reviewer.

The final correction's actual same-viewer DEV browser Retry exercise also passed. Reviewer read `/private/tmp/axis-inbox-release/dev-retry-browser-result.json`: both initial 401 and 403 cases remain refused after another explicit request, then reveal the list after the next explicit successful Retry, with one additional SMS request per Retry. Both report `mobileOverflow: false`. Root additionally reports visibility refocus did not bypass refusal and retained screenshots. This supplies the previously pending real-browser R1 edge; test interception changed responses, not authorization code or production data.

Root's first retained portal E2E run encountered stale private `.env.test` account settings, then passed 10 of 12 cases after using canonical QA accounts. Two failures were a missing email-thread fixture and a resident document context replaced during its normal stage redirect. Root reports an insert-only exact synthetic DEV fixture correction, with no provider send or existing-row overwrite. The full 12-case rerun is pending; the earlier failed run is not counted as passing E2E. Final staging deployment/feature QA, preflight, and release verification remain root-owned subsequent gates. Do not promote based on this report alone while required gates are pending.

Root relayed the corrected actual staging rollback rehearsal before this handoff: exit 0, all seven fixture-leak counts zero, full pre/post catalog exactly equal, and the double-install/assertion matrix retained. It used migration hash `0848ed305b3fa742f1b16e75133931718c7c05b10f7b4df760d7ded2467d53a4` and private wrapper hash prefix `bbbcf475`. This is corrected-source staging rehearsal evidence; it is not deployed staging feature QA or production application. Root also checkpointed the candidate as local commit `542d11ca0`; all 33 frozen hashes remain unchanged. Source acceptance follows those same bytes into the checkpoint.

Mandatory Bugbot evidence is retained separately in `docs/security/2026-09-13-inbox-release-acceptance-bugbot.md`; independent security review is root-coordinated. The four older production prerequisites have their own report, `docs/security/2026-09-13-prp472-production-prerequisites-review.md`. This acceptance grants no additional production mutation authority and does not certify those separate applications. Any changed source bytes require affected re-review and validation.

## Focused test-harness addendum

Root added one mechanical E2E-only correction to `tests/e2e/mobile-portal-layout.spec.ts:106`: retry the resident `page.evaluate` measurement for up to 10 seconds when navigation destroys the document context. The overflow result is still collected and asserted outside that retry. A successful measurement of `true` completes immediately and remains a failing overflow result; it cannot be retried until false. The route iteration, landmark wait, 390x844 viewport, 2px tolerance, and final overflow assertion are unchanged. **Focused review accepted with no actionable finding.**

At this addendum reviewer independently rechecked the original 33 frozen paths: zero mismatches. The additional test path has SHA-256 `d9bf6975fad0404c0ba88954c8de6efe7b316d1ba63831602ca75b69813c036e`; root will capture the expanded 34-path freeze. Current checkpoint remains `542d11ca0cc20aaf6edb2d7141fa4d2d29afb3f5` plus this test delta and documentation. Product/UI/SQL source bytes are unchanged, so the completed broad product validation remains applicable. Root owns the full E2E rerun and scoped harness lint/typecheck; those results are pending at this addendum. No implementation or test file was edited by this reviewer.

## Composer maintenance review and observed QA failure

Read the bounded composer maintenance plan and fresh Sol handoff, then reviewed the added `tests/e2e/communication-reply-composer.spec.ts` delta against the actual frozen `ResidentDirectChatPane`, `InboxComposer`, and `InboxComposerScheduleMenu` implementations. **The test maintenance is accepted.** The optional fixture label retains the CI default, and the exact `resident-direct-chat-compose` textarea plus ancestor-form scope prevents silently testing a different pane. The test still exercises real click/focus and filechooser events, empty/draft Send state without submission, desktop emoji insertion, intentionally hidden phone emoji, and actual schedule configure/cancel with pressed-state assertions. The 36px phone attachment minimum matches the current shared control, while clickability and existing textarea/Send geometry assertions remain. No file is selected or uploaded and no send is submitted. The prior mobile measurement correction remains accepted.

The composer test path SHA-256 is `cb394180c74ef0cd975cabe37fab7ef69312c49ff341772732e8ae19192317f3`. Reviewer again rehashed all original 33 paths with zero mismatches; the two additional E2E paths make the eventual expanded inventory 35 paths, pending root capture. Sol reports composer ESLint and scoped diff check exit 0; its graph refresh attempt exited 1 for the existing local executable-resolution issue.

**The full retained E2E suite is not accepted as passed.** During this review root reported that the new run reached the preserved textarea geometry assertion and measured **129px at a 375px viewport**, failing the existing `>140px` requirement. This is a measured geometry failure, not a stale selector or document-context exception. It requires resolution or an explicit, justified test-contract decision before calling this suite green. No assertion was relaxed by this reviewer.

Root considered repeating the suite with the deployed/default SMS-disabled flag. Such a run can validate that deployment configuration, but source inspection shows it does not remove the shared direct-pane Send via control: `replyChannelMenu` is unconditionally included in `ResidentDirectChatPane` and `InboxComposerChannelMenu` always renders its phone-width trigger even with `smsAvailable=false`. Therefore disabling SMS must not be assumed to repair the same direct-pane geometry, and a different generic inbox pane is not equivalent coverage. Subsequent root QA/resolution evidence remains pending. This addendum does not identify when the width regression was introduced or claim a product-source correction has been made.
