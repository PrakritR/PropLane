# PRP-470 independent Astra review

Date: 2026-09-13. Verdict: **changes required; return to Sol for correction cycle 1.** No release, merge, commit, or push approval is given.

## Exact scope

Worktree: `/Users/akhilvemuri/.treehouse/AXIS-2-ea44f0/6/AXIS-2`.
Branch: `akhil/prp-470-inbox-loading`.
Base and current HEAD independently verified: `7b3d464fe2f102a527949044fc0289513d50029f`.
Reviewed uncommitted changes to `pro-communication.tsx`, `pro-unified-inbox.tsx`, `resident-communication.tsx`, `portal-inbox-storage.ts`, `manager-applications-storage.ts`, three existing inbox test files, and the new `communication-inbox-initial-state.tsx` and `inbox-initial-loading-readiness.test.tsx`. The plan and execution handoff were reviewed as evidence, not treated as proof by themselves. No implementation file was changed by this review.

Read the repository/Akhil instructions, feature-cycle contract, ship gate, Communication/UI contracts, web/native parity, and installed Next client/caching guides. This pool checkout has neither `.graphify/graph.json` nor `graphify-out/graph.json`; source review followed the area documents. The executor's required graph refresh failed with `could not determine executable to run` (exit 1). No global tooling or other worktree graph was altered.

## Findings

1. **P2 / Medium: an old viewer's SMS authorization response can permanently block the new viewer's entire inbox.** `src/components/portal/pro-unified-inbox.tsx:317` handles 401/403 by setting `smsPollHaltedRef` before the viewer/generation check at line 326. That halt is never reset when the viewer changes. `loadInitialList` now requires `smsOk` at line 380, so an old account can prevent an authenticated new account from completing initial load, and Retry returns false at line 315 without requesting SMS again. Bugbot independently reproduced this using deferred responses: A pending, B SMS500, late A401, B Retry with valid next response. Actual `{ smsCalls: 2, errorStillVisible: true }`; expected a third request and recovery. The temporary regression exited 1 and was removed. Existing SMS-only halt behavior has been widened into whole-inbox unavailability by this change. Guard all status side effects before publication and reset/own the latch per viewer generation.

2. **P2 / Medium, pre-existing defect in an explicit acceptance boundary: previously loaded empty SMS contacts survive account changes.** `src/components/portal/pro-unified-inbox.tsx:333` merges `pendingOptimistic` from all current `smsResidents`; lines 345-351 retain a row with `savedContactName` and no messages even when B's successful server result contains none of A's contacts. The new viewer effect at line 240 increments an epoch but does not clear or scope already published state. Once B's initial sources complete, A's saved name/phone can appear in B's list. Late-response rejection alone does not fix this. Retain optimistic contacts only within their owning viewer generation, preserving same-viewer contact-create behavior. This defect predates PRP-470; the proposed readiness/isolation guarantee and its A-to-B test matrix are still incomplete while it remains.

3. **P3 / Low: evidence overstates the runtime used for validation.** `docs/plans/2026-09-13-prp-470-handoff.md` says all commands used Node 22. Root observed the full-unit worker executable at `/opt/homebrew/Cellar/node/23.7.0/bin/node` for PID 69024. Preserve the successful suite result, remove the blanket runtime claim, and state the verified runtime per subsequent command. No need to rerun 10,017 tests solely to relabel this historical evidence.

4. **P3 / Low: retry lacks the required interaction analytics attribute.** The new Retry Button in `src/components/portal/communication-inbox-initial-state.tsx:22` has no `data-attr`, despite the plan and repository convention. Add a stable, non-PII attribute in the small correction.

## Acceptance and evidence assessment

The main initial-load fix is sound in the exercised paths: the manager waits for inbox, applications, and enabled SMS; resident waits for inbox and enabled SMS; successful empty responses differ from errors; local/session hydration does not claim a successful server load. The callback refreshes the parent directory in the same promise completion as ready state. Source inspection supports preserving deep links, hiding provisional counts and list choices, and keeping normal background refresh separate from initial loading. Legacy array-returning APIs remain available.

The 13 new tests were inspected. They cover inbox identity publication, late inbox401, A-B-A inbox slot ownership, malformed inbox rows, manager deferred inbox/SMS, manager A-B-A late SMS success, applications/inbox completion order, malformed manager SMS, disabled manager SMS and empty archive, manager retry, and resident deferred inbox with SMS disabled. They do **not** establish the stale SMS401 boundary, previously published optimistic contacts on account change, resident enabled-SMS failure/identity handling, or same-viewer post-ready failure stability. These gaps matter because two of them produced the findings above. Correction tests should target those behaviors without expanding into an unrelated suite rewrite.

Independently opened the actual manager loading, loaded, and mobile-deep-link screenshots. Loading has skeleton rows and no provisional selectable list/counts. Loaded shows nine conversations (the segment badge excludes the assistant), real task conversations, and the directory-backed fixture described by the handoff. Reviewed source for the resident/error render paths and inspected the six artifact paths on disk. The executor reports browser delays of real dev/test responses, nine-row manager completion, six-row resident completion, directory-only contact, failure/retry, pending deep link, mobile list-first behavior, and warm return. Root separately viewed all six images. These are seeded dev/test checks, not `/demo` proof; no actual email/SMS send was part of acceptance.

At review time, the existing `inbox-cycle` browser session showed `chrome-error://chromewebdata/` and `ERR_CONNECTION_REFUSED` for localhost. The snapshot command exited 0 but the application was not reachable; this is not a passing fresh browser retest. Correction execution must restore the pinned 3009 server before the final Review URL handoff.

Executor command evidence: focused 12 files/62 tests exit 0; full unit 1,423 files/10,017 tests exit 0 before the final SMS epoch change; final readiness 13 tests exit 0; full lint, impacted ESLint, enlarged-heap typecheck, build, diff check, and preflight reported exit 0. Default-heap typecheck initially exited 134, then passed with 8 GB. Final narrow checks appropriately followed the last SMS change. Runtime attribution is qualified above. This reviewer independently ran `git diff --check` successfully and the bugbot ran the failing targeted probe described above. No broad suite was rerun without a new reason. Full local portal E2E and staging QA remain release prerequisites; no promotion is requested or approved.

## Cache, rendering, performance, native and UX review

- No server/RSC boundary, route, database schema, RLS, message-send authorization, attachment handling, native plugin, or navigation registry changed. Shared web/Capacitor rendering remains the implementation; no native-shell rebuild is implied by these files. A browser mobile viewport is not simulator evidence.
- Successful-read timestamps now determine readiness TTL, and failed/malformed responses do not manufacture cache success. Inbox requests capture viewer key/generation and only remove their own promise slot. Initial calls use the existing unforced dedupe path; no broad forced prefetch or new polling source was added.
- The loading UI uses existing tokens and row shapes, one status label, `aria-busy`, and reduced-motion handling. Retry returns a promise to the shared Button. Background SMS starts after readiness. No new heavy dependency was introduced.
- Baseline mobile UX issue remains visible: `output/playwright/prp470/manager-mobile-deep-link.png` shows a long stored draft squeezed into roughly 140px beside attachment, AI, schedule, channel, and send controls. The relevant reply composer and AI-draft adoption in `src/components/portal/pro-inbox.tsx:2327`/`:2390` are unchanged by this diff. This reviewer did not create or clear the draft. Record this as an existing narrow-composer follow-up, not a PRP-470 regression or a reason for broad redesign in this correction.

Required independent reports: `docs/security/2026-09-13-prp-470-security.md` and `docs/security/2026-09-13-prp-470-bugbot.md`. Both were completed and read by this reviewer. Bugbot also independently ran the existing readiness, polling, unified-conversation and resident-conversation tests via the explicit Node22 executable: exit 0, four files/28 tests. Its failing temporary regression establishes that this green suite misses finding 1. Security established no new Critical/High issue; its two Medium findings match findings 1 and 2 above. The application request-options concern was investigated and not raised: current resident callers omit managerUserId, so switching to the manager loader's explicit viewer changes the scope and clears its success timestamp/promise. Correction plan: `docs/plans/2026-09-13-prp-470-correction-1.md`.

**Release verdict: blocked pending the scoped identity corrections, their regression evidence, and a fresh Astra review.** No source fix, commit, push, tracker mutation, production operation, or no-mistakes invocation was performed by this reviewer.
