# Correction cycle 1 - communication billing release

Fresh Sol-medium implements this plan under the original release plan and review. Workdir on every command: `/Users/akhilvemuri/.treehouse/AXIS-2-ea44f0/3/AXIS-2`. No no-mistakes, Linear, Lavish, credentials, remote/provider actions, Git mutations, or production approval. Root owns broad checks and release. Do not run a competing compiler. At most two automated correction cycles overall.

## Ownership

- **Terra:** new fixed apply runner, its unit test, and a new dedicated local runner integration harness/test. Own structural verification and all transport/waiver tests. Preserve preparation runner, six-source bundle, canonical correction, consumed recovery scripts/SQL/rehearsal/CA exactly. Do not add a CLI target override or generic SQL input.
- **Luna:** shared `portal-inbox-ui.tsx` composer layout and narrowly associated browser regression only. Start from root's actual 375px reproduction; read UI checklist and relevant Next/ frontend instructions. Preserve all controls and existing phone/nav behavior. Do not redesign the inbox or alter unrelated compose fields.
- **Sol:** subscription checkout route and a focused route authorization test, integration review, candidate runner digest in the still-DRAFT waiver, and correction handoff. Resolve enrollment intent against actual callers before adding the canonical manager guard; do not implement subscription-session redesign. No delegate overlaps these files.

## Acceptance and verification

1. New runner's readback checks effective column privileges, exact valid/unique index shape and relation, and exact relevant constraints. Negative tests must reject column-only anon/authenticated grants, a same-name nonunique/wrong-key index, and weakened constraints. Preserve exact ledger/priorstate semantics and conservative unknown classification.
2. Use disposable localhost PostgreSQL with real `pg` for the new runner transport, reusing fixture setup read-only from the old rehearsal where suitable. Execute the verified interior and auxiliary insert in one transaction. Inject failure at auxiliary insert and before-commit catalog validation; independent readback must show no source or auxiliary ledger rows or new objects. Exercise confirmed COMMIT and lost-response uncertainty with no replay. Never use staging as the first write rehearsal.
3. Exercise real-driver asynchronous disconnect/timeout and TLS rejection (wrong hostname/untrusted certificate) using local fixtures. Test production-fixed TLS config separately if the test harness needs a localhost test CA; do not weaken operational CA/host validation. Ensure timed-out operations cannot later produce a reported success or leave the test process hanging.
4. Spawn the entrypoint from an isolated fixture copy with a fake local CLI executable. Assert exact pinned command/target, private modes, environment isolation from hostile PG/SSL/.env settings, cleanup on success/failure, and sanitized stdout/stderr. Fixture APPROVED with matching digest reaches the injected safe boundary; DRAFT, consumed, wrong hash, missing/mixed acknowledgements refuse before connection. Never edit the real waiver to approve it. Add source/bundle/CA drift refusal tests in copies, with specific expected failures.
5. Manager checkout must reject resident/vendor-only users before billing/provider side effects while permitting a manager multi-role user using canonical role access. Keep the authenticated-owner identity binding and paid-plan behavior. Existing idempotency follow-up is outside correction scope.
6. Browser proof on real seeded nonproduction data: textarea normal center click, typing, emoji, attachments, send/schedule control reachability at 375/768/1280px, plus safe-area/native phone navigation preserved. No provider send without separately authorized QA scope. Record exactly which flows ran and which remain blocked.
7. Run narrow changed tests/lint and local runner harness with actual exit codes, then hand results to root for broad validation scheduling. Record new runner hash and leave waiver DRAFT. Request fresh Astra review through root, not a release action. Keep all immutable hashes unchanged and report graph refresh constraints honestly.
