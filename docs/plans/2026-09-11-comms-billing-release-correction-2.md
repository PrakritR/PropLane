# Correction cycle 2 - bounded test and CI completion

This is the second and final automated correction cycle under the original release plan. Fresh Sol-medium manages Terra/Luna, then returns to fresh Astra review. Every command uses `/Users/akhilvemuri/.treehouse/AXIS-2-ea44f0/3/AXIS-2`. HEAD remains `97815a2d459c0bf96e9e80d4f94d29bff7d23fe2` with the pending merge. Read correction-1 review/handoff and updated root evidence first.

No runtime/source redesign, Git mutation, credential acquisition, remote/provider/database/deployment action, waiver approval, no-mistakes, Linear, or Lavish. Preserve the fixed runner at `56e88313a0da9b14ef04e4d146392782a79085e80ff04266149e8a6f9be09cc6`, all frozen preparation/bundle/correction/consumed artifacts/CA, and the actual DRAFT waiver. If a runtime defect appears, report evidence instead of expanding scope.

## Ownership

- Terra owns only `scripts/testing/comms-billing-migration-apply-local-harness.mjs`, its unit wrapper, and `tests/unit/comms-billing-migration-apply-subprocess.test.ts`.
- Luna owns only `tests/e2e/communication-reply-composer.spec.ts`, fixing the observed fixture and selectors. Product component and analytics attributes are out of scope.
- Sol owns `.github/workflows/test.yml`, a concise local harness prerequisite note in the correction-2 handoff, integration review and validation coordination. Keep the existing unit job/check relationship; no broad CI redesign.

## Acceptance

1. Retain the assistant-thread/banner normal-click regression at all three widths. On a deterministic seeded eligible ordinary conversation, test schedule checkbox/date/uncheck plus textarea typing, emoji, attachment chooser and send enablement at 375/768/1280. Use scoped accessible roles and file inputs or derive the actual prefix. Observe `resident-direct-chat-compose` on Test Resident. Assistant scheduling is deliberately absent. No forced clicks, provider sends, uploads or scheduled submissions. Clear drafts. Use real Playwright test execution when root releases the browser slot; otherwise root explicitly owns executing the final exact spec and recording exit/result before completion. Collection is not a pass.
2. In timeout/disconnect harness cases, execute the actual bundle interior successfully, assert it completed, then inject the real statement timeout/backend termination before COMMIT (for example at auxiliary insertion). Assert one completed interior, one injection, zero COMMIT, and fresh independent clean catalog/ledger. Preserve auxiliary/catalog rollback, successful COMMIT/readback, real catalog-negative and TLS tests, and lost-COMMIT-response uncertainty/no-replay. Count actual completed interior execution separately from attempted queries. No operational target/TLS override.
3. Source/bundle drift copied-fixture tests assert intended sanitized error text as well as exit 1/no fake CLI invocation. Preserve actual-waiver DRAFT and all subprocess isolation/cleanup tests.
4. Make the existing mandatory unit job explicitly install a fixed PostgreSQL server major with the required extension tools and OpenSSL (use Ubuntu 24.04 with PostgreSQL 16 packages for a stable apt baseline), then expose `/usr/lib/postgresql/16/bin` via GITHUB_PATH and print harmless tool versions before the unit command. Run clusters as the ordinary CI user, not root. Keep the unit wrapper unconditional; no skip on missing tools. Document macOS/local equivalent prerequisites and PATH, and provide a clear fail-fast diagnostic if binaries are missing. No external database or secrets required. A localhost disposable database remains a hermetic dependency when its binaries are provisioned. Do not claim hosted CI execution unless observed; inspect workflow structure and run the full local harness with explicit binary path.
5. Coordinate with root before any test process to avoid its active build. Use Node22.23.0, 4GB heap, one Vitest worker, narrow changed tests only. Report final harness and narrow tests/scoped lint/Playwright exact outcomes. Root owns broad compiler/build/full suites. Recompute protected identities; expected fixed runner and waiver digest stay unchanged. Record files, evidence, remaining external gates and exact fresh-review prompt in `docs/plans/2026-09-11-comms-billing-release-correction-2-handoff.md`.

No third automated correction cycle. If findings remain after fresh review, report them to Akhil with precise evidence and incomplete release gates.
