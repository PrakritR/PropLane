# PRP-448/449 final bugbot review - 2026-09-09

Status: no open correctness findings in the reviewed patch.

Resolved findings:

- A lease update and its notification intent commit or roll back together.
- Database read, claim, event-load, and finalization errors are surfaced instead
  of being counted as healthy delivery.
- The lease CAS uses the stored timestamp and advances it monotonically, so a
  same-millisecond write cannot reuse the prior token.
- SQL null comparisons fail closed for required event and delivery fields.
- Resident user id and normalized email cannot redirect a delivery away from
  the lease recipient; the canonical recipient key is also checked.
- Required manager and resident signature notifications cannot be silently
  omitted from their corresponding transitions.
- The worker includes newly enqueued pending rows, prevents overlapping workers
  from finalizing each other's claims, and retries after a crashed claim lease.
- Correct manager and resident detail links are built from the server-resolved
  application base URL and the lease id.
- The RPC writes the already-authorized normalized lease row without changing
  the existing document, signature-role, or per-signature hash rules.

The crash-retry contract is at-least-once delivery with deterministic downstream
message ids. It does not assert carrier-level exactly-once delivery after an
external provider accepts a request and the process dies before recording the
final state.

Source inventory hashes are recorded in the paired security report. The final
implementation-lane focused run reported 23 passing tests.

Final coordinator re-review after integration fixes: base `6f24d93b712f140d16af5f10e14b1fd88edd61aa`; source/tests/area-doc diff SHA-256 `c44b8b2e1585cf6cec50e85100e08c75d9d286f93793d5708d3d3209872a0b14`; migration SHA-256 `234e3df42ac4cf4a3f5912635095af6d82b1792cde31a56c24a479db42d70636`. Final focused validation passed 92 tests; TypeScript, targeted ESLint and diff-check exited 0. Partial email/SMS retry preserves completed legs even after a retry exception; deferred due times survive; each sequential row gets a fresh claim; submitted provider work is counted separately. Resident-triggered notifications use the owning manager work-number identity while retaining triggeringActorUserId in the event payload. The dev-only migration and rollback-only database checks passed; real two-party signing and provider receipt remain pending.
