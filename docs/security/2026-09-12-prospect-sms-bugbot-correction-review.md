# Prospect SMS bugbot correction review

Date: 2026-09-12. Fresh Astra review after correction cycle1. Branch `prospect-agent-eval-loop`, HEAD `6f24d93b712f140d16af5f10e14b1fd88edd61aa`. This is normal code review, not no-mistakes.

**Verdict: changes required.** Root's subsequent `docs/plans/prospect-sms-retired-transport-amendment.md` resolves the scope conflict: keep Claw retired and remove the new managed linkage. The corrected scope is active manager-owned Twilio; the remaining implementation findings still require correction.

1. **P1 - Gateway completion is claimed for an unreachable retired transport.** `src/lib/claw-messenger.server.ts:35` returns false unconditionally; the real webhook at `src/app/api/webhooks/claw-messenger/route.ts:54` cannot ingest under any environment. This matches `docs/agents/sms-system.md:895`. New tests mock configuration true. If enabled by a later decision, the new managed outbox delegates to a helper that resends ambiguous outcomes with fresh IDs (`src/lib/claw-messenger.server.ts:231`, `:261`), so one outbox call can create duplicate provider sends. The parallel security reviewer reproduced that latent defect and a fail-open managed-Claw consent lookup. Resolve the actual authorized rail before activation; do not silently reactivate retired transport.
2. **P2 - Runtime shadow grounding and paired primary identity remain incomplete.** `src/lib/agent/leasing-sms-agent.server.ts:392` omits burst ID/revision on the primary trace; `:511` emits only tool calls while the scorer requires facts at `src/lib/agent/prospect-shadow-comparison.ts:88`. Every real job therefore has unknown grounding even with checkable recorded outputs. Fixture-only scorer success does not establish runtime comparison behavior.
3. **P2 - Incident integration tests still mock the behavior they claim to prove.** `tests/unit/prospect-sms-burst-callback.test.ts:106`, `:136`, `:142`, and `:145` stub the complete handler's success/silence result. They establish forwarding, not canonical retrieval/tour follow-up, legitimate repeat handling, or correction during actual pending generation. Add a real runtime/provider-stub fixture.

C1 atomic preparation/lock order/crash recovery, C3 local claim release, and C4 honest suppressed escalation appear corrected. Root's isolated final SQL hash `f2e77728990800838696e6fff60df17c723861dd9555a217f0650dc7ee96be70` passed 78 checks plus clean/repeat application. The reviewer inspected the actual source and behavioral tests; root/Sol command evidence is attributed to them, not recast as independent test execution. Final broad validation and browser evidence were pending at report time. External QStash, designated SMS recipient, paid GPT, staging, manager takeover, and graph tooling limits remain explicit.

Detailed review: `docs/plans/prospect-sms-single-reply-review1.md`. Bounded final correction plan: `docs/plans/prospect-sms-single-reply-correction2.md`. No implementation, shared database, external messaging, paid model, release, PR, or git mutation was performed by this reviewer.
