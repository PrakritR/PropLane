# SMS rollout final correction review

Date: 2026-09-12. Fresh Astra review against branch `prospect-sms-release`, base HEAD `a4967b835880ad44b13b10c1fcc112de80081fbe`, and the supplied activation plan, implementation/correction handoffs, and earlier Bugbot/migration security reviews.

## Decision

Accept F1/F2 corrections and the pinned private migration runner for the bounded three-file apply, subject to its live gates passing. No blocking finding remains in this reviewed scope. This is acceptance of source and operational guard design, not a claim that staging QA, database application, deployment, or release has completed. Root retains those responsibilities and must preserve the staging-before-production ladder.

The final reviewed private `migrate.mjs` SHA-256 is `8bb983fdfab9c4f50c04e10dac805825a70548d402dd07ff4db257ed3827ad0e`. The intermediate finite-timestamp fix was pinned at `2635ae92e22effdcb3b54d5961ef82fd909ada852ff8038be86774d864b04611`; the final bounded revision adds the target-specific gate checks below. Earlier runner pin `a495248a97bf6846ee25d1d64acd0f6d32e5ea9d3a6bde347c387cac0108e8d2` was superseded during this review: missing or malformed `active.updatedAt` could bypass a comparison against NaN. Root added an explicit finite published-timestamp check, resolving that finding. Any further runner change needs bounded review of the new pin.

## Source correction verification

- F1 resolved: the actual `loadManagerPlanAddonQuantities` error branch now always returns `ok: false`; the missing-relation/schema-cache exception is gone. Successful empty reads retain known zero quantities. The real `loadWorkspacePlan` combines base-plan and add-on read failures into `unknown`; create returns 503 before invoking its creation RPC. Tests cover ordinary timeout, both missing-schema messages, empty success, and the route refusal.
- F2 resolved: Remove includes `!row.purchasable` in its disabled condition and uses the same unavailable title as Add. The rendered positive-quantity test verifies no mutation fetch after clicking the disabled control. The shared mutation helper still returns unavailable before billing or entitlement dependencies, with no reactivation toggle.
- SQL is unchanged. Independently recomputed pins match the reviewed manifest: payment preferences `aacb7a687588c15f479f3e8fc87ddae3d40d17c8de7b8229c98defde9890fe74`; add-ons `586ca8c1fe00fb254335ade510f53781b55bd74a93fc0b12cb0db22070ef72d0`; atomic workspace RPC `0c1924b8a0e2339d6bb6ae370ff2823badb5374bde51f157b788ac09b3cfe36c`.

## Private apply and audit path

Private artifacts remain under `/private/tmp/proplane-sms-activation/`; no credentials or backup contents are reproduced here. `db-common.mjs` remains pinned at `cc74aefea9e476171fa6416545d790959591e0fc8a092d5382f07757147a1f4a`. Independent offline checks verified both catalog/schema backup hashes against their target manifests and all three source hashes against the migration manifest. Both saved CLI dry runs list exactly those three files, with no seeds or roles. Exact target allowlists, initial catalog equality, pending identity checks, historical replay guards, source/mirror equality, and post-apply identity/ACL/function verification remain intact.

The gate requires the exact active deny rule and both POST path groups, enabled firewall, no draft, a finite activation and publication timestamp, activation at or after publication, and at least 830 seconds elapsed. It then reprobes both target paths and requires HTTP 403 plus exactly `x-vercel-mitigated: deny`. Generic response text no longer qualifies. Saved evidence shows only this rule active, published at `2026-09-12T22:52:34.441Z`, with all four path/host probes denied at verification time `2026-09-12T22:52:46.125Z`. For this unchanged publication, earliest eligible application is `2026-09-12T23:06:36.125Z`. Republishing invalidates this timestamp and requires a fresh verified activation/drain under the reviewed runner.

Root supplied the official Vercel maximum-duration evidence and deployed-source inspection (no route/config override at deployed `01c6af066`). Independently inspected private project evidence shows Fluid enabled and a 300-second default. The chosen conservative 800-second ceiling plus 30-second margin is accepted on that supplied platform evidence; this reviewer made no network calls.

New ledger statements must now match the pinned source using existing `normalizeSql`. This normalizer folds comments, whitespace, and semicolons outside quoted strings; it is canonical text comparison, not a complete SQL parser or proof of identical statement boundaries. Exact installed function-body equality separately covers the dollar-quoted function contents, while raw source hashes, guarded CLI files, and catalog/ACL checks protect the actual apply. That combination is adequate for these three pinned sources. Keep the previously recorded post-apply inspection of exact column defaults/check definitions and business-count differences; do not treat regex checks alone as exhaustive catalog parity. Keep gates closed on partial application and inspect committed history before retrying.

## Independent verification and remaining QA

An offline Node VM exercised the actual final `firewallGate` function with controlled dependencies: elapsed valid gate accepted; missing/malformed publication timestamp, later republication, generic 403 without denial header, wrong status, and an unpublished draft all rejected. Exit 0. Independent backup/source hash verification and `git diff --check` also exited 0. No database, external API, or customer message was used by this reviewer.

Root reports the pre-correction full unit suite passing (10,001 tests), plus 24 affected tests and 42 inbound-email tests after correction. Correction handoff records scoped lint exit 0 with one existing hook warning, and stopped TypeScript/full-suite attempts accurately as exit 130. These are attributed results, not independently rerun here. Root build/lint and real staging browser/API QA remain outstanding at report time.

The tiny UI condition/title change introduces no new fetch, cache key, force refresh, timer, analytics event, route, navigation item, upload behavior, or native branch. It preserves existing component geometry and semantic button behavior. The server authoritative refusal also protects stale clients. No portal parity registry change is required: the shared web component and server boundary cover native shells as before. Verify positive-quantity disabled controls, keyboard order, and compact rendering during real staging QA. Prefer narrowing the temporary gate for staging QA with a fresh production drain over adding a special continuity bypass or replacing full staging route QA with mocked evidence.

Graphify unavailability and absence of a pooled graph are documented in the preceding review/handoff. This documentation-only review made no source change and did not rerun graph generation. The only reviewer edit is this report.

## Final bounded gate-narrowing amendment

The final runner pin above accepts the original two-condition path/POST groups only for staging. Production requires three conditions including non-negated `environment eq production`, and reads the distinct private `production-gate-activation.json`. The finite publication/activation checks, full 830-second drain, active rule check, and fresh exact 403/deny probes remain mandatory. Production cannot reuse the initial broad-gate activation file. Root will apply staging first, deploy the guarded candidate, narrow to production, record a fresh verified production activation, and use that new drain interval for staging browser/API QA before production application.

Independently executed the actual final gate function offline for both targets against original, production-only, wrong-environment, and negated-environment rule shapes. All eight target/shape checks behaved as expected (exit 0), including target-specific activation filename assertions. The initial earliest-apply timestamp in this report now applies only to staging; production must compute its own fresh deadline after narrowing. Root supplied installed Vercel CLI schema evidence for the environment condition; live production denial and staging reachability remain root verification steps. This bounded amendment is accepted with no new blocking finding.
