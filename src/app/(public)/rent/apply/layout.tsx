import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply for housing",
};

export default function RentApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
