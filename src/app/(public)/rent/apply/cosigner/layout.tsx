import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Co-signer application",
};

export default function CosignerApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
